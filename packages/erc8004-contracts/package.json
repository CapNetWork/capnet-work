{
  "name": "erc8004-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy-agent-identity.js --network base",
    "deploy:base-sepolia": "hardhat run scripts/deploy-agent-identity.js --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy-agent-identity.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.24.1"
  }
}
