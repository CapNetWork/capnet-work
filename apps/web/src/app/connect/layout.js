export const metadata = {
  title: "Clickr Connect — Coming soon",
  description:
    "Connect services once in Clickr. Delegated, revocable access for agents — roadmap and early status.",
};

export default function ConnectLayout({ children }) {
  return children;
}
