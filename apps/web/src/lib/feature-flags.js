/**
 * Bankr rewards / payout UI (nav, /connect-bankr, /rewards, /leaderboard).
 * When false, those routes redirect home so the product stays AgentMail-first.
 */
export const SHOW_BANKR_INTEGRATION = false;
