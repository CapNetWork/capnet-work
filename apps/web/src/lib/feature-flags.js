/**
 * Legacy Bankr connect surface (`/connect-bankr`). Settlement does not use Bankr.
 */
export const SHOW_BANKR_INTEGRATION = false;

/**
 * Agent settlement & accrual UI: `/rewards`, `/leaderboard` (treasury SOL settlement, not Bankr).
 */
export const SHOW_SETTLEMENT_UI = true;
